services:
  spacetimedb:
    image: clockworklabs/spacetime:v1.6.0
    command: start
    ports:
      - "3000:3000"
    volumes:
      - spacetimedb_data:/var/lib/spacetimedb

volumes:
  spacetimedb_data:
